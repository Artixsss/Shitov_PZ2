<Window x:Class="Shitov_PZ2.Task5"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Задача 5" Height="900" Width="600">
    <Grid Background="#FFF0FFF6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Задание: Сгенерировать массив размером M столбцов и N строк, содержащий числа от -10 до 10. Отобразить его пользователю. Отсортировать элементы в нем по возрастанию и по убыванию. Вывести результаты сортировки в двух таблицах. Найти максимальный и минимальный элементы массива."
                   TextWrapping="Wrap" Grid.Row="0" Margin="10"/>
        <TextBlock Text="Введите размеры массива (M и N) через пробел:" Grid.Row="1" Margin="10"/>
        <TextBox x:Name="txtInput" Grid.Row="2" Margin="10"/>
        <Button Content="Генерировать и сортировать" Grid.Row="3" Margin="10" Click="btnGenerateAndSort_Click" Background="#FF678A71"/>
        <TextBlock x:Name="txtResult" Grid.Row="4" Margin="10"/>
        <TextBlock x:Name="txtMinMax" Grid.Row="5" Margin="10"/>
    </Grid>
</Window>

